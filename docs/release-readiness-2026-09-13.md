# مرشح النشر — 13 سبتمبر 2026

الحالة: منشور على الإنتاج. أكد المستخدم نجاح عودة اللاعب بعبارة «ايي عاد»، ثم نشر المرشح المختبر. هذا يؤكد العودة فعليًا؛ دليل انتقال المضيف وبقية تفاصيل المراحل هو الاختبارات الآلية، دون نسبة تقرير تفصيلي غير وارد للمستخدم.

النشر: Vercel `dpl_DiP7unxyFtXLeLA1nU6rWvUJRkkP` بحالة READY، والرابط الرئيسي https://mafia-night-iota.vercel.app. دالة `mafia-room` الرئيسية نسخة 48 بحالة ACTIVE. تقرير فحص ما بعد النشر محفوظ في `artifacts/production-assurance.json`.

## الإنجاز والأدلة

| المحور | النتيجة |
|---|---|
| الأمان وسرية الأدوار | جدول الفئات وحصر الإجراءات موثقان في assurance-2026-09-13.md؛ اختبارات الجلسات المسحوبة وانتحال الهوية وأوامر المضيف وتسريب الأسرار ناجحة. تقوية صلاحيات الإنتاج مطبقة؛ صفر منح غير مخولة للجداول والدوال. |
| القواعد | جدول الأدوار وترتيب الحسم والفوز والخروج موثق؛ 95 حالة مستقلة للتفاعل والصلاحيات والفوز نجحت ضمن مجموعة 91 اختبارًا. مباريات آلية بتشكيلات مختلفة مكتملة. |
| الثبات | استعادة المراحل الخمس ورفض الأوامر أثناء الانقطاع ناجحان في المتصفح؛ اختبارات انتقال المضيف والطلبات القديمة والتكرار ناجحة. اختبار فقد استجابة الانضمام يثبت استرجاع المقعد نفسه. نتيجة الجوالين الفعليين معلقة. |
| الحفظ والاستعادة | أخطاء حفظ المقعد أو الملف ترجع المعاملة كاملة؛ الانضمام المتزامن يرجع نفس المقعد. pg_dump/pg_restore إلى قاعدة منفصلة نجحا مع تطابق الجداول الثلاثة عشر والعلاقات والصلاحيات والتسلسلات. التنظيف التجريبي أبقى الغرف النشطة والحديثة والإحصاءات غير المسجلة والملفات والمواسم. |

الأدلة المحلية:
- `artifacts/release-gate-final.log`: 91/91، صفر إخفاق.
- `artifacts/role-interactions.json`: 95 حالة تفصيلية داخل أحد الاختبارات.
- `artifacts/native-concurrency-final.log`: خمسة اختبارات عبر اتصالات PostgreSQL مستقلة.
- `artifacts/phase-recovery-final.log` و`artifacts/phase-recovery.json`: خمس مراحل، صفر أخطاء متصفح، واستعادة استجابة انضمام ضائعة.
- `artifacts/backup-restore/report.json`: استعادة فعلية لبيانات اختبار وتنظيف تجريبي ناجح.
- `artifacts/live-acceptance-api.json`: 318 طلبًا إلى endpoint القبول المقيد بالغرفة التجريبية، بما فيها مشاهدون على شبكة واحدة.
- `artifacts/preview-ready.json`: تحميل وتسجيل دخول النسخة المنشورة النهائية دون أخطاء.

أرشيف الاستعادة الأخير: `test-only-6bdd7ffcc1b5.dump`، SHA-256 `7fb01b4100ffc5890cb165de80e9087302a9066adbfe6073b9dbc8823a242735`.

## تغييرات الإنتاج المكتملة

- `20260913115325_mafia_service_only_grants`: سحب المنح غير اللازمة.
- `20260913121414_atomic_join_seat`: معاملة انضمام قابلة لإعادة المحاولة، متوافقة مع الخادم السابق الذي لا يستعملها بعد.
- `mafia-room-acceptance` نسخة 2: نفس منطق المرشح مع بوابة قبول محصورة بالغرفة التجريبية ومهلة صلاحية. لا تنفذ إنشاء غرف إنتاجية أو قراءة ملفات عامة.
- نشر Vercel التجريبي `dpl_84XxXSd6Fr3Q7sMiG3xGYXJNMpPH` بحالة READY. نسخة المستخدم السابقة تبقى متاحة لإكمال التجربة دون فقد التخزين المحلي بسبب اختلاف النطاق.

## المرشح المجمد وخطوة النشر

الخادم المجمد `.test-assurance/production-edge.ts` ببصمة SHA-256 `7e27a88dd082d58b07fe877b068c8cc42b47588ea4d5347d88101819b5453b4e`.
الواجهة المجمدة `.test-assurance/production` تشمل 58 ملفًا. manifest يحفظ بصمة كل ملف ويمنع سكربت النشر رفع ملف تغير بعد الفحص. المجلد مستثنى من Git ويحتوي بيانات اختبار خاصة؛ لا ينشر المجلد نفسه.

نفذ نشر الخادم المجمد إلى mafia-room، ثم `node scripts/publish-assurance.mjs production`. نسخة الخادم السابق محفوظة محليًا للاسترجاع. لا تؤخذ تعديلات متزامنة جديدة من مجلد العمل دون إعادة تقييم المرشح.

الاختبارات الآلية لا تثبت تعليق تطبيق على هاتف فعلي. لم يفعّل تنظيف الإنتاج، ولم تنفذ استعادة بيانات إنتاج أو PITR. لا تتضمن هذه المهمة قياس توازن اللعبة بمباريات بشرية طويلة؛ ذلك منفصل عن تطابق القواعد الوظيفي المطلوب.
